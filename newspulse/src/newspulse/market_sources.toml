# NewsPulse curated market source list (DEC-1 B, the curated half).
#
# The three classes a news feed never carries: studies, regulatory dates and
# events. This file is editable — add, remove or reclassify a source without
# touching code. Each entry is a `[[sources]]` table with:
#   name  (required) — human label, and the publisher credited on a signal from a
#                      source that does not name its own
#   url   (required) — the RSS/Atom feed URL
#   kind  (required) — one of: studie, regulierung, veranstaltung
#
# This is the half of the radar that applies to every mandate. The other half is
# a per-mandate search in the client's own field, built in market_sources.py and
# stored with origin = "suche", so a reader can always tell the two apart.
#
# The list is global on purpose for now: a mandate that wants its own trade
# association added is a settings change once the shape here has proven itself.
#
# Every URL below was fetched and parsed before it was written here, and the
# entry count it returned is in the comment beside it. The first version of this
# file was not: seven of its twelve feeds answered 404 on the first sweep that
# used them, and because `collect` had no per-source boundary the first failure
# ended the whole class — twenty-one errors on a run that had material in hand.
# Five of those seven publish no feed that could be found: destatis and its own
# advertised feed URL both 404, ZEW, BaFin, AUMA and the BDI advertise none.
# They are gone rather than left in to fail every morning. A source that cannot
# be verified does not belong in a curated list.
#
# Reachability is still not checked in the test suite (nothing here touches the
# network). A source that goes dark now logs at WARNING and is skipped; only a
# class whose every source failed is an ERROR, which is the claim that was
# always meant.
#
# Only feed-provided fields are ever ingested. Full article bodies are never
# fetched or stored (Leistungsschutzrecht) — see ingest.py.

# --- Studien: who measures the German market -----------------------------------

[[sources]]
name = "ifo Institut"
url = "https://www.ifo.de/rss.xml"          # 50 Einträge, geprüft 2026-08-26
kind = "studie"

[[sources]]
name = "DIW Berlin"
url = "https://www.diw.de/de/rss_news.xml"  # 20 Einträge, geprüft 2026-08-26
kind = "studie"

[[sources]]
name = "Bitkom"
url = "https://www.bitkom.org/rss.xml"      # geprüft 2026-08-26
kind = "studie"

# --- Regulierung: what is coming, and when it lands -----------------------------

[[sources]]
name = "Bundesregierung"
url = "https://www.bundesregierung.de/service/rss/breg-de/1151244/feed.xml"
kind = "regulierung"

[[sources]]
name = "Bundestag"
url = "https://www.bundestag.de/static/appdata/includes/rss/aktuellethemen.rss"
kind = "regulierung"

[[sources]]
name = "Bundeskartellamt"
# The one the site itself advertises; the path in the first version of this file
# was not a page that has ever existed.
url = "https://www.bundeskartellamt.de/DE/Service/RSS/_documents/rssnewsfeed.xml"  # 30 Einträge
kind = "regulierung"

[[sources]]
name = "Europäische Kommission"
url = "https://ec.europa.eu/commission/presscorner/api/rss?language=de"
kind = "regulierung"

# --- Veranstaltungen: where this field is staged --------------------------------
#
# One entry, and it is the only verified events feed of the several this class
# was written with. The class leans on the per-mandate field search for the rest,
# which is a thinner answer than the other two get and worth saying out loud
# rather than papering over with sources that 404.

[[sources]]
name = "DIW Berlin Veranstaltungen"
url = "https://www.diw.de/de/rss_events.xml"  # 20 Einträge, geprüft 2026-08-26
kind = "veranstaltung"
